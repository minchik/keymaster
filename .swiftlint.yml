# SwiftLint config for keymaster (single-file CLI).
# Install: brew install swiftlint   |   Run: swiftlint

included:
  - keymaster.swift

disabled_rules:
  - todo
  - trailing_whitespace

line_length:
  warning: 120
  error: 160

function_body_length:
  warning: 80
  error: 120
